<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MusicPlayer" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Status bar color -->
        <item name="android:statusBarColor" tools:targetApi="26">@color/white</item>
        <item name="android:navigationBarDividerColor" tools:targetApi="26">@color/white</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:windowLightNavigationBar">true</item>
    </style>

    <style name="Theme.MusicPlayer" parent="Base.Theme.MusicPlayer">
        <item name="colorPrimary">@color/white</item>
        <item name="colorOnPrimary">@color/gray</item>
        <item name="colorSecondary">@color/dark</item>
        <item name="colorOnSecondary">@color/soft_green</item>
        <item name="selectableItemBackground">@color/light_green</item>
    </style>

</resources>